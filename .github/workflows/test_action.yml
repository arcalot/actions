on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:

jobs:
  test_action:
    runs-on: ubuntu-latest
    name: Test KinD action
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Kind Action Test
        uses: ./kind
